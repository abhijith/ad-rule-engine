#### Dependencies

* Leiningen 2.8.1
* Java 9

#### Cloning and installing the application

	$ git clone
	$ cd rpm
	$ lein deps

#### Running the code

* Starting the application

		$ cd rpm
		$ lein run
		$ curl http://localhost:4567

* Tests

		$ lein test
